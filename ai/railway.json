{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ai && npm install && cd server && npm install",
    "watchPatterns": ["ai/**"]
  },
  "deploy": {
    "startCommand": "cd ai/server && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}