[phases.setup]
nixPkgs = ['nodejs_20', 'ffmpeg']

[phases.install]
cmds = [
  'cd ai/server',
  'npm ci --omit=dev'
]

[variables]
PUPPETEER_SKIP_DOWNLOAD = 'true'
PUPPETEER_EXECUTABLE_PATH = '/usr/bin/chromium'

[start]
cmd = 'cd ai/server && node index.js'
